import styled from "styled-components";

const Btn = styled.button`
  background-color: coral;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 8px;
  cursor: pointer;
`;

export default Btn;
